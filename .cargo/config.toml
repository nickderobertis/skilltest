# Build configuration every cargo invocation anywhere inside this clone reads.
#
# `target-dir` here is resolved relative to the directory holding `.cargo/` (the
# parent of the `.cargo` directory), so every crate in this clone — workspace
# members and any crate outside the workspace alike — builds into
# `<clone>/target`, and a second clone or worktree of this repository, carrying
# its own copy of this file, builds into its own root. Never point this outside
# the clone: concurrent worktrees must not share one target directory.
[build]
target-dir = "target"

# Line tables only for dev and test builds (`test` inherits `dev`): backtraces and
# coverage keep file:line, and the full debuginfo cargo writes by default
# (`debug = 2`) stays off the disk. `release` is untouched.
[profile.dev]
debug = 1
